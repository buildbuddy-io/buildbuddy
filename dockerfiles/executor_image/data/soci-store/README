This binary was built from source from the https://github.com/awslabs/soci-snapshotter repo.

To build the binary, checkout the repo and run `make soci-store`. The built binary will be at out/soci-store.
