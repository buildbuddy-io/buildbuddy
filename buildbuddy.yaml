actions:
  - name: Memtest
    triggers:
      push:
        branches:
          - "master"
      pull_request:
        branches:
          - "*"
    bazel_commands:
      - run :memtest
    resource_requests:
      memory: 10Gi
