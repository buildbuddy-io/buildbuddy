actions:
  - name: Test
    container_image: ubuntu-20.04
    triggers:
      push:
        branches:
          - "master"
          - "bb_release_*"
      pull_request:
        branches:
          - "*"
    bazel_commands:
      - version
      - build //server/util/lru/...
      - build //server/util/lru/...
