build:local --bes_results_url=http://localhost:8080/invocation/
build:local --bes_backend=grpc://localhost:1985

build:dev --bes_results_url=http://dev.app.buildbuddy.io/invocation/
build:dev --bes_backend=grpc://dev.app.buildbuddy.io:1985
