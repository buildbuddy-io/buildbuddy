syntax = "proto3";

package api.v1;

// Request object for GetFile
message GetFileRequest {
  // This corresponds to the uri field in the File message.
  string uri = 1;
}

// Response object for GetFile
message GetFileResponse {
  // The file data.
  bytes data = 1;
}

// A file associated with a BuildBuddy build.
message File {
  string name = 1;
  string uri = 2;
  string hash = 3;
  int64 size_bytes = 4;
}

// Request object for DeleteFile
message DeleteFileRequest {
  // TODO - do we want to take a URI here? It might make more sense to take a digest + remote instance name
  string uri = 1;
}

// Response object for DeleteFile
message DeleteFileResponse {}