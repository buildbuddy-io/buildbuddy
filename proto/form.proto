syntax = "proto3";

package form;

// An error associated with a user form field.
message FieldError {
  // Name of the form field which should match the corresponding proto field
  // name.
  string field_name = 1;

  // Error message that should be shown above the field.
  string error_message = 2;
}
